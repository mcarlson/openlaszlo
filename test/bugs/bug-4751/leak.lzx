<canvas>

    <dataset name="dsLeak" src="test-contactsdata.xml" type="http"/>

    <attribute name="loadDel" 
               value="$once{ new LzDelegate( dsLeak, 'doRequest' ) }"/>

    <method event="oninit">
        loadDel.execute();
    </method>

    <method event="ondata" reference="dsLeak">
        var dp = dsLeak.getPointer();
        //van n = mydataset.xpathQuery( ... )
        LzTimer.addTimer( loadDel, 1000);
    </method>

</canvas>
